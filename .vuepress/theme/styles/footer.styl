.footer
  color lighten(desaturate($color-primary, 30%), 30%)
  display flex
  flex-direction column
  margin 0 2rem 1rem 2rem
  .meta
    padding-bottom 1rem
    display flex
    flex-direction row
    justify-content space-between
  .text
    border-top 1px solid lighten(desaturate($color-secondary, 60%), 60%)
    padding-top 1rem
    text-align center
